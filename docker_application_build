# Build
FROM golang:1.26-alpine3.23 AS builder
ARG app

WORKDIR /build
COPY . .
RUN echo building app : ${app}
RUN go -C app/web/${app} build -o main 

# Alpine Linux 
FROM alpine:3.23

ARG app
RUN addgroup -S tarantula && adduser -S tarantula -G tarantula

RUN mkdir /home/tarantula/${app}
 

USER tarantula
WORKDIR /home/tarantula
#RUN chown -R tarantula /home/tarantula  

RUN mkdir /home/tarantula/.ssh
RUN chown -R tarantula /home/tarantula/.ssh  
COPY --from=builder /build/.gitconfig .
RUN chown tarantula /home/tarantula/.gitconfig

COPY --from=builder /build/id_ed25519 ./.ssh
COPY --from=builder /build/known_hosts ./.ssh
COPY --from=builder /build/gx-01.json ./.ssh
COPY --from=builder /build/gx-01.key ./.ssh
COPY --from=builder /build/gx-01.key.pub ./.ssh

RUN chown tarantula ./.ssh/id_ed25519
RUN chown tarantula ./.ssh/known_hosts

# Application Install
WORKDIR /home/tarantula/bin
RUN echo installing app : ${app}
COPY --from=builder /build/app/web/${app}/main .
COPY --from=builder /build/app/web/${app}/*.json .

RUN chown tarantula ./main
RUN chown tarantula ./*.json

ENTRYPOINT ["./main"]