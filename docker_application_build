# Build
FROM golang:1.24.2-alpine AS builder
ARG app
WORKDIR /build
COPY . .
RUN echo building app : ${app}
RUN mkdir site
RUN if [ ${app} = "admin" ]; then cp -r cmd/web/${app}/site site; fi
RUN go -C cmd/web/${app} build -o main 

# Alpine Linux 
FROM alpine:latest
RUN apk add --no-cache openssh
RUN apk add --no-cache git
ARG app
RUN addgroup -S tarantula && adduser -S tarantula -G tarantula
RUN mkdir /etc/tarantula 
COPY cmd/web/${app}/${app}-conf.json /etc/tarantula/${app}-conf.json
RUN mkdir /home/tarantula/${app}
RUN mkdir /home/tarantula/.ssh

RUN chown -R tarantula /home/tarantula/.ssh
COPY id_ed25519 /home/tarantula/.ssh
RUN chown tarantula /home/tarantula/.ssh/id_ed25519
RUN chown -R tarantula /home/tarantula/${app}   

# Application Install 
USER tarantula
WORKDIR /home/tarantula/bin
RUN mkdir site
COPY --from=builder /build/cmd/web/${app}/main .
COPY --from=builder /build/site/* site

ARG h
ARG n
ARG s
ARG g
RUN echo ${h} ${n} ${s} ${g}
ENV TN_HOST=${h}
ENV TN_NAME=${n}
ENV TN_ID=${s}
ENV TN_GROUP=${g}
ENTRYPOINT ["./main"]