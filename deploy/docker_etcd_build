FROM alpine:3.21

RUN mkdir -p /var/tarantula/etcd/data
RUN mkdir -p /etc/tarantula/etcd
RUN apk add --no-cache etcd

# copy configuration
COPY etcd.config /etc/tarantula/etcd/etcd.config

ENTRYPOINT ["etcd", "--config-file=/etc/tarantula/etcd/etcd.config"]